---
interface Props {
  n: number;
  of: number;
}
const { n, of } = Astro.props;
const pad = (value: number): string => String(value).padStart(2, '0');
---

<span class="buoa-index"><span class="buoa-index__current">{pad(n)}</span> / {pad(of)}</span>
