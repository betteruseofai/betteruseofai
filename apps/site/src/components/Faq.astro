---
/**
 * Questions worth asking.
 *
 * Native details, one per question, none open by default. The heading is a
 * question mark, which is the one place STYLE.md allows one.
 */
interface Props {
  /** Pass an empty string where the page already carries a heading. */
  title?: string;
}
const { title = 'Questions worth asking' } = Astro.props;
---

<div class="stack">
  {title ? <h2 class="band__title">{title}</h2> : null}
  <div class="buai-faq measure">
    <slot />
  </div>
</div>
