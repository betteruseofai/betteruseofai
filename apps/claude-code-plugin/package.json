{
  "name": "@betteruseofai/claude-code-plugin",
  "version": "0.0.0",
  "private": true,
  "description": "Claude Code plugin: a live footprint on the status line and a nudge when a smaller model would do.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node scripts/bundle.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  },
  "dependencies": {
    "betteruseofai": "workspace:*"
  }
}
